# LangRS — local / CI development install (default Rex-oriented core + tests).
# PyPI installs use metadata from setup.py (install_requires + extras).
#
# Default detection backend (Rex-Omni) is vendored inside `langrs` and uses the
# core requirements in `requirements-core.txt`. Optional heavy GPU extras
# (e.g. flash-attn / vLLM) are exposed via setuptools extras, for example:
#   pip install -e ".[rex-omni]"

-r requirements-core.txt
-r requirements-dev.txt
